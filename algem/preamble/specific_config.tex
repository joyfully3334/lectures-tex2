%%% Шаблонная информация для титульного листа
\newcommand{\TitleCourseName}{АЛГЕБРА И ГЕОМЕТРИЯ}
\newcommand{\SemesterNumber}{II}
\newcommand{\LecturerInitials}{Богданов Илья Игоревич}
\newcommand{\CourseDate}{весна 2026}

\newcommand{\ifzr}[3]{\ifnum #1=0 #2 \else #3 \fi}

%%% Колонтитулы
\usepackage{titleps}
\newpagestyle{main}{
	\setheadrule{0.4pt}
  \sethead{\hyperlink{intro}{$\uparrow$}}{\ifzr{\value{section}}{}{\thesection}\sectiontitle}{\thepage}
}
\pagestyle{main}

%%% Счетчики
% \setcounter{chapter}{-1}  % Нумерация глав с нуля
\setcounter{secnumdepth}{1} % Отключение нумерации подразделов

\renewcommand{\thesection}{\S\arabic{section}}

\counterwithout{theorem}    {chapter}
\counterwithin*{theorem}    {section}
\counterwithout{lemma}      {chapter}
\counterwithin*{lemma}      {section}
\counterwithout{proposition}{chapter}
\counterwithin*{proposition}{section}
\counterwithout{definition} {chapter}
\counterwithin*{definition} {section}

\counterwithin{equation}{section}
\renewcommand{\theequation}{\arabic{chapter}.\arabic{section}.\arabic{equation}}

%%% Пометки о начале лекций
\newcommand\lectureinfo[2]{
  \tk[overlay]{
    \tp{(current page.east |- 0,0)+(-20mm, 0)
    node[anchor=east, color=black!50] {\footnotesize{Лекция #1 (#2)}}}
  }
}

%%% Свои значки в \itemize
\setlist[itemize,1]{label=$*$}
\setlist[itemize,2]{label=$-$}
\setlist[itemize,3]{label=$\centerdot$}
\setlist[itemize,4]{label=$\cdot$}
